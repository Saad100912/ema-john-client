import React from "react";

const NotFound = () => {
    return (
        <div>
            <h2>The webpage you are trying to access doesn't exist. RIP!!!</h2>
        </div>
    );
};

export default NotFound;
